syntax = "proto3";
package pb;

message Behavior {
	enum Idle {
		unknownIdle = 0;
		stand = 1;
		wander = 2;
		patrol = 3;
	}
	enum Movement {
		unknownMovement = 0;
		grounded = 1;
		aerial = 2;
		climbing = 4;
	}
}

message Mind {
	Behavior.Idle idle = 1;
	Behavior.Movement movement = 2;
}
