syntax = "proto3";
package pb;

import "pose.proto";

message CharacterSprite {
	Pose.Type pose = 1;
	string path = 2;
	repeated uint32 step = 3;
}

message CharacterAsset {
	string name = 1;
	repeated CharacterSprite sprite = 2;
}

message AssetManifest {
	repeated CharacterAsset character = 1;
}
